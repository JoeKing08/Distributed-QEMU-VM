[完成] 移除兼容优先约束，按 manifest/typed runtime 作为唯一生产路径收口纲领与入口
[进行中] 把 manifest-driven typed dispatch 接入统一 node_runtime/executor 边界；入口 context 已统一，旧数据面投影仍待移除
[进行中] 接入基础 membership join/cordon/drain/gateway replacement 控制面入口；已完成可靠有序 typed control stream、认证 actor 边界、CTRL_RESULT 回传及 replay/拒绝回归，并补齐不复制 membership 状态的正式 control-owner Unix listener 生命周期与并发/EOF/stop 回归；`ctl_tool serve` 已接入同一 durable admission/membership/control plane，使用 Unix peer UID 到 typed principal 的显式映射。核实：`wvm_admission_orchestrator_run` 已是完整 begin→prepare→route/reservation/participant→decide_activation→commit 事务且带 abort 补偿，但生产 `wvm_ctl` 的 `apply_create_vm` 未装 admission builder（`admission_input_builder` 恒为 NULL），故当前 CREATE_VM 仅完成 durable begin + replay/conflict，随后因 `prepare_input==NULL` 走 abort 返回 PRECONDITION_FAILED（`test_ctl_create_vm` 正断言此语义），完整 prepare/activate/commit 事务尚未在生产路径运行。已定序：按 A 先接 membership 生命周期入口（join/validate/prepare/commit 使节点真正成 ACTIVE、control plane 具备真实 inventory/records），再让 CREATE_VM builder 从真实 record set 取快照驱动完整事务。核实（2026-08-25）：membership apply service callback 已在生产路径就位——`wavevm_control_service.c:14-22` 的 `control_plane_apply` 无条件接入每个 `wvm_ctl serve` transport（`:51-52`，`apply=control_plane_apply`、`apply_opaque=config->plane`），转发到 `wvm_control_plane_membership_apply`→`wvm_membership_control_apply`，并强制 `membership_config` 已装的 self/executor 授权器；`wvm_control_service_config` 有意不含 `apply` 字段（由 service 层统一供给，非各调用方自填）。故先前"service_config.apply 未接"的假设不成立，无需另写 callback。`test_control_plane_membership.sh`/`test_ctl_service.sh`/`test_control_membership_records.sh` 均通过。下一步（交回原 AI）：让 CREATE_VM admission builder 从真实 membership record set 取快照驱动完整 prepare→activate→commit 事务
[待办] 完成 Mode B 多 VM 隔离与生命周期验证
[待办] 实现 per-VM Mode A kernel context 并验证双 VM 隔离
[待办] 按 flat/fractal 与 TCG/KVM 合同执行分层验证
[证据 2026-08-22] `make -C ctl_tool -B` 通过；`test_control_transport.sh`、`test_control_owner.sh`、`test_control_service.sh`、`test_ctl_service.sh`、`test_control_plane.sh`、`test_executor_session.sh`、`test_control_plane_membership.sh` 通过；新增 `test_ctl_create_vm.sh` 通过首次 durable identity、相同 request-id 字节级 replay、语义冲突拒绝和 daemon 重启 replay。
