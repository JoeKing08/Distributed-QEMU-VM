[完成] 移除兼容优先约束，按 manifest/typed runtime 作为唯一生产路径收口纲领与入口
[进行中] 把 manifest-driven typed dispatch 接入统一 node_runtime/executor 边界；入口 context 已统一，旧数据面投影仍待移除
[进行中] 接入基础 membership join/cordon/drain/gateway replacement 控制面入口；已完成可靠有序 typed control stream、认证 actor 边界、CTRL_RESULT 回传及 replay/拒绝回归，并补齐不复制 membership 状态的正式 control-owner Unix listener 生命周期与并发/EOF/stop 回归；`ctl_tool serve` 已接入同一 durable admission/membership/control plane，使用 Unix peer UID 到 typed principal 的显式映射。核实：`wvm_admission_orchestrator_run` 已是完整 begin→prepare→route/reservation/participant→decide_activation→commit 事务且带 abort 补偿，但生产 `wvm_ctl` 尚未绑定真实 `wvm_admission_authority`，故当前 CREATE_VM 仅完成 durable begin + replay/conflict，随后在缺少 prepare provider 时 fail-closed（`test_ctl_create_vm` 正断言此语义），完整 prepare/activate/commit 事务尚未在生产路径运行。已移除 `ctl_tool` 内永远为 NULL 的旧 builder fallback，避免重新引入第二条生产入口。已定序：membership 生命周期入口已具备（join/validate/prepare/commit 使节点真正成 ACTIVE、control plane 具备真实 inventory/records）；capability/profile 与 inventory/reservation 的 evidence owner 已完成，flat/fractal route snapshot compiler 已完成；controller-owned launch/listener plan provider 已完成，发布时要求完整 membership record set、精确 node instance 绑定和已有 lease storage，且可绑定 coordinator prepare options。本地 participant receiver 已具备 stage 解码、controller 身份/路由键/预留绑定校验和 runtime-gate 交接：解码存储完全由调用方限界提供，交付完整持久化后才激活 gate，且防止未中止的 runtime prepare 直接释放 reservation。下一步是将该 receiver 与跨节点 route/participant transport 接入真实 owner/provider，再让 CREATE_VM builder 从 capture 的真实 record set 驱动完整事务。核实（2026-08-25）：membership apply service callback 已在生产路径就位——`wavevm_control_service.c:14-22` 的 `control_plane_apply` 无条件接入每个 `wvm_ctl serve` transport（`:51-52`，`apply=control_plane_apply`、`apply_opaque=config->plane`），转发到 `wvm_control_plane_membership_apply`→`wvm_membership_control_apply`，并强制 `membership_config` 已装的 self/executor 授权器；`wvm_control_service_config` 有意不含 `apply` 字段（由 service 层统一供给，非各调用方自填）。故先前"service_config.apply 未接"的假设不成立，无需另写 callback。`test_control_plane_membership.sh`/`test_ctl_service.sh`/`test_control_membership_records.sh` 均通过。核实（2026-08-29）：全量 `tests/control-plane/*.sh` 回归中控制面、membership、coordinator、runtime、隔离及现有 CREATE replay/fail-closed 测试通过；修复 `test_control_route_transaction_journal.sh` 遗漏的 membership 链接依赖。旧 `test_resource_plan.sh` 仍是历史 `--plan` fixture，与 manifest-only 目标不兼容，应在替代 manifest 测试接入后移除，而不是恢复旧 CLI。
[待办] 完成 Mode B 多 VM 隔离与生命周期验证
[待办] 实现 per-VM Mode A kernel context 并验证双 VM 隔离
[待办] 按 flat/fractal 与 TCG/KVM 合同执行分层验证
[证据 2026-08-22] `make -C ctl_tool -B` 通过；`test_control_transport.sh`、`test_control_owner.sh`、`test_control_service.sh`、`test_ctl_service.sh`、`test_control_plane.sh`、`test_executor_session.sh`、`test_control_plane_membership.sh` 通过；新增 `test_ctl_create_vm.sh` 通过首次 durable identity、相同 request-id 字节级 replay、语义冲突拒绝和 daemon 重启 replay。
[证据 2026-08-31] 新增 `wavevm_admission_provider.[ch]`：controller-owned launch/listener plan publication 要求完整 membership record set、精确 `(physical_node_id, node_instance_id)` 绑定、唯一节点和已有 lease storage；可将已发布计划绑定到 coordinator prepare options，并作为 orchestrator 的 `prepare_input` owner 提供同一模板。`test_admission_provider.sh`、`test_admission_evidence_owner.sh`、`test_admission_route.sh`、`test_control_plane.sh` 及 5 个 ctl/service 测试通过；全量脚本并行运行会触发共享 `ctl_tool/wvm_ctl` 构建竞态，后续回归应串行执行。`test_executor_session.sh` 当前仓库权限为 0644，需显式用 bash 调用；`test_resource_plan.sh` 仍为旧 `--plan` fixture，返回 usage/退出 2，不能恢复旧 CLI。
[证据 2026-09-01] `wavevm_admission_receiver.[ch]` 已收紧 participant/reservation 接收端的 prepare→commit→activate 边界：调用方提供所有解码存储，未再在收包路径按协议上限分配；完整 runtime delivery 成功后才 bind/activate gate；已激活实例不接受旧 prepare 重置；还保证 runtime prepare 必须先 abort 才能释放对应 reservation。`wavevm_admission_receiver.c` 以 `-Werror` 独立编译通过；`test_runtime_gate.sh`、`test_admission_route.sh`、`test_lifecycle_projection.sh` 及 `make -C ctl_tool -B` 通过。它还未接入 production control transport，下一步需配套真实 owner/provider 与 E2E receiver 测试。
