按 docs 纲领依赖顺序继续把 WaveVM 从当前中间态落地到完整架构并完成对应验证。执行约束：manifest/typed runtime 是唯一生产路径；没有存量业务，不保留旧 master/slave 独立入口、旧 48-byte legacy wire header、NODE/ROUTE 注入、旧 field-by-field IOCTL 或兼容部署；不添加硬编码或测试专用 workaround，不启动未经验证的最终 guest E2E。依次完成：移除兼容优先约束并收口纲领；将manifest- driven typed dispatch 接入统一 node_runtime/executor 边界；接入基础 membership join、compute cordon/drain、gateway replacement 控制面入口；完成 Mode B 多 VM 隔离与生命周期验证；实现 per-VM Mode A kernel context 并验证双 VM 隔离；最后按 flat/fractal 与 TCG/KVM 语义合同执行分层验证。
